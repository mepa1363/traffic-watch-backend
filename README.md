# Traffic Watch Backend API

## Build Setup

```bash
# install dependencies
npm install

# serve with hot reload with nodemon at localhost:3000
./run-dev.sh

# run for production with pm2
./run-prod.sh

```
